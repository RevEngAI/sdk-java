/*
 * RevEng.AI API
 * RevEng.AI is an AI-powered binary analysis platform for reverse engineering and malware analysis. It provides similarity search across executable binaries and functions, AI-driven decompilation, dynamic execution analysis, firmware unpacking, and integration with external threat intelligence sources like VirusTotal.
 *
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package ai.reveng.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import ai.reveng.invoker.JSON;

/**
 * FunctionMatchingFilters
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen")
public class FunctionMatchingFilters {
  public static final String SERIALIZED_NAME_BINARY_IDS = "binary_ids";
  @SerializedName(SERIALIZED_NAME_BINARY_IDS)
  @javax.annotation.Nullable
  private List<Integer> binaryIds = new ArrayList<>();

  public static final String SERIALIZED_NAME_COLLECTION_IDS = "collection_ids";
  @SerializedName(SERIALIZED_NAME_COLLECTION_IDS)
  @javax.annotation.Nullable
  private List<Integer> collectionIds = new ArrayList<>();

  public static final String SERIALIZED_NAME_FUNCTION_IDS = "function_ids";
  @SerializedName(SERIALIZED_NAME_FUNCTION_IDS)
  @javax.annotation.Nullable
  private List<Long> functionIds = new ArrayList<>();

  public static final String SERIALIZED_NAME_USER_IDS = "user_ids";
  @SerializedName(SERIALIZED_NAME_USER_IDS)
  @javax.annotation.Nullable
  private List<Integer> userIds = new ArrayList<>();

  /**
   * Gets or Sets debugTypes
   */
  @JsonAdapter(DebugTypesEnum.Adapter.class)
  public enum DebugTypesEnum {
    USER("USER"),
    
    SYSTEM("SYSTEM"),
    
    EXTERNAL("EXTERNAL"),
    
    UNKNOWN_DEFAULT_OPEN_API("unknown_default_open_api");

    private String value;

    DebugTypesEnum(String value) {
      this.value = value;
    }

    public String getValue() {
      return value;
    }

    @Override
    public String toString() {
      return String.valueOf(value);
    }

    public static DebugTypesEnum fromValue(String value) {
      for (DebugTypesEnum b : DebugTypesEnum.values()) {
        if (b.value.equals(value)) {
          return b;
        }
      }
      return UNKNOWN_DEFAULT_OPEN_API;
    }

    public static class Adapter extends TypeAdapter<DebugTypesEnum> {
      @Override
      public void write(final JsonWriter jsonWriter, final DebugTypesEnum enumeration) throws IOException {
        jsonWriter.value(enumeration.getValue());
      }

      @Override
      public DebugTypesEnum read(final JsonReader jsonReader) throws IOException {
        String value =  jsonReader.nextString();
        return DebugTypesEnum.fromValue(value);
      }
    }

    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      String value = jsonElement.getAsString();
      DebugTypesEnum.fromValue(value);
    }
  }

  public static final String SERIALIZED_NAME_DEBUG_TYPES = "debug_types";
  @SerializedName(SERIALIZED_NAME_DEBUG_TYPES)
  @javax.annotation.Nullable
  private List<DebugTypesEnum> debugTypes = new ArrayList<>();

  public FunctionMatchingFilters() {
  }

  public FunctionMatchingFilters binaryIds(@javax.annotation.Nullable List<Integer> binaryIds) {
    this.binaryIds = binaryIds;
    return this;
  }

  public FunctionMatchingFilters addBinaryIdsItem(Integer binaryIdsItem) {
    if (this.binaryIds == null) {
      this.binaryIds = new ArrayList<>();
    }
    this.binaryIds.add(binaryIdsItem);
    return this;
  }

  /**
   * ID&#39;s of binaries to limit the search to, if empty, search all scoped binaries
   * @return binaryIds
   */
  @javax.annotation.Nullable
  public List<Integer> getBinaryIds() {
    return binaryIds;
  }

  public void setBinaryIds(@javax.annotation.Nullable List<Integer> binaryIds) {
    this.binaryIds = binaryIds;
  }


  public FunctionMatchingFilters collectionIds(@javax.annotation.Nullable List<Integer> collectionIds) {
    this.collectionIds = collectionIds;
    return this;
  }

  public FunctionMatchingFilters addCollectionIdsItem(Integer collectionIdsItem) {
    if (this.collectionIds == null) {
      this.collectionIds = new ArrayList<>();
    }
    this.collectionIds.add(collectionIdsItem);
    return this;
  }

  /**
   * ID&#39;s of collections to limit the search to, if empty, search all scoped collections
   * @return collectionIds
   */
  @javax.annotation.Nullable
  public List<Integer> getCollectionIds() {
    return collectionIds;
  }

  public void setCollectionIds(@javax.annotation.Nullable List<Integer> collectionIds) {
    this.collectionIds = collectionIds;
  }


  public FunctionMatchingFilters functionIds(@javax.annotation.Nullable List<Long> functionIds) {
    this.functionIds = functionIds;
    return this;
  }

  public FunctionMatchingFilters addFunctionIdsItem(Long functionIdsItem) {
    if (this.functionIds == null) {
      this.functionIds = new ArrayList<>();
    }
    this.functionIds.add(functionIdsItem);
    return this;
  }

  /**
   * ID&#39;s of functions to limit the search to, if empty, search all scoped functions
   * @return functionIds
   */
  @javax.annotation.Nullable
  public List<Long> getFunctionIds() {
    return functionIds;
  }

  public void setFunctionIds(@javax.annotation.Nullable List<Long> functionIds) {
    this.functionIds = functionIds;
  }


  public FunctionMatchingFilters userIds(@javax.annotation.Nullable List<Integer> userIds) {
    this.userIds = userIds;
    return this;
  }

  public FunctionMatchingFilters addUserIdsItem(Integer userIdsItem) {
    if (this.userIds == null) {
      this.userIds = new ArrayList<>();
    }
    this.userIds.add(userIdsItem);
    return this;
  }

  /**
   * ID&#39;s of users to limit the search to, if empty, search all scoped users
   * @return userIds
   */
  @javax.annotation.Nullable
  public List<Integer> getUserIds() {
    return userIds;
  }

  public void setUserIds(@javax.annotation.Nullable List<Integer> userIds) {
    this.userIds = userIds;
  }


  public FunctionMatchingFilters debugTypes(@javax.annotation.Nullable List<DebugTypesEnum> debugTypes) {
    this.debugTypes = debugTypes;
    return this;
  }

  public FunctionMatchingFilters addDebugTypesItem(DebugTypesEnum debugTypesItem) {
    if (this.debugTypes == null) {
      this.debugTypes = new ArrayList<>();
    }
    this.debugTypes.add(debugTypesItem);
    return this;
  }

  /**
   * Limit the search to specific debug types, if empty, search all scoped debug &amp; non-debug functions
   * @return debugTypes
   */
  @javax.annotation.Nullable
  public List<DebugTypesEnum> getDebugTypes() {
    return debugTypes;
  }

  public void setDebugTypes(@javax.annotation.Nullable List<DebugTypesEnum> debugTypes) {
    this.debugTypes = debugTypes;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the FunctionMatchingFilters instance itself
   */
  public FunctionMatchingFilters putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }


  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    FunctionMatchingFilters functionMatchingFilters = (FunctionMatchingFilters) o;
    return Objects.equals(this.binaryIds, functionMatchingFilters.binaryIds) &&
        Objects.equals(this.collectionIds, functionMatchingFilters.collectionIds) &&
        Objects.equals(this.functionIds, functionMatchingFilters.functionIds) &&
        Objects.equals(this.userIds, functionMatchingFilters.userIds) &&
        Objects.equals(this.debugTypes, functionMatchingFilters.debugTypes)&&
        Objects.equals(this.additionalProperties, functionMatchingFilters.additionalProperties);
  }

  @Override
  public int hashCode() {
    return Objects.hash(binaryIds, collectionIds, functionIds, userIds, debugTypes, additionalProperties);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class FunctionMatchingFilters {\n");
    sb.append("    binaryIds: ").append(toIndentedString(binaryIds)).append("\n");
    sb.append("    collectionIds: ").append(toIndentedString(collectionIds)).append("\n");
    sb.append("    functionIds: ").append(toIndentedString(functionIds)).append("\n");
    sb.append("    userIds: ").append(toIndentedString(userIds)).append("\n");
    sb.append("    debugTypes: ").append(toIndentedString(debugTypes)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    return o == null ? "null" : o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("binary_ids", "collection_ids", "function_ids", "user_ids", "debug_types"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(0);
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to FunctionMatchingFilters
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!FunctionMatchingFilters.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field(s) %s in FunctionMatchingFilters is not found in the empty JSON string", FunctionMatchingFilters.openapiRequiredFields.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // ensure the optional json data is an array if present
      if (jsonObj.get("binary_ids") != null && !jsonObj.get("binary_ids").isJsonNull() && !jsonObj.get("binary_ids").isJsonArray()) {
        throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `binary_ids` to be an array in the JSON string but got `%s`", jsonObj.get("binary_ids").toString()));
      }
      // ensure the optional json data is an array if present
      if (jsonObj.get("collection_ids") != null && !jsonObj.get("collection_ids").isJsonNull() && !jsonObj.get("collection_ids").isJsonArray()) {
        throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `collection_ids` to be an array in the JSON string but got `%s`", jsonObj.get("collection_ids").toString()));
      }
      // ensure the optional json data is an array if present
      if (jsonObj.get("function_ids") != null && !jsonObj.get("function_ids").isJsonNull() && !jsonObj.get("function_ids").isJsonArray()) {
        throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `function_ids` to be an array in the JSON string but got `%s`", jsonObj.get("function_ids").toString()));
      }
      // ensure the optional json data is an array if present
      if (jsonObj.get("user_ids") != null && !jsonObj.get("user_ids").isJsonNull() && !jsonObj.get("user_ids").isJsonArray()) {
        throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `user_ids` to be an array in the JSON string but got `%s`", jsonObj.get("user_ids").toString()));
      }
      // ensure the optional json data is an array if present
      if (jsonObj.get("debug_types") != null && !jsonObj.get("debug_types").isJsonNull() && !jsonObj.get("debug_types").isJsonArray()) {
        throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `debug_types` to be an array in the JSON string but got `%s`", jsonObj.get("debug_types").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!FunctionMatchingFilters.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'FunctionMatchingFilters' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<FunctionMatchingFilters> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(FunctionMatchingFilters.class));

       return (TypeAdapter<T>) new TypeAdapter<FunctionMatchingFilters>() {
           @Override
           public void write(JsonWriter out, FunctionMatchingFilters value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additional properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else {
                   JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                   if (jsonElement.isJsonArray()) {
                     obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                   } else {
                     obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                   }
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public FunctionMatchingFilters read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             JsonObject jsonObj = jsonElement.getAsJsonObject();
             // store additional fields in the deserialized instance
             FunctionMatchingFilters instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of FunctionMatchingFilters given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of FunctionMatchingFilters
   * @throws IOException if the JSON string is invalid with respect to FunctionMatchingFilters
   */
  public static FunctionMatchingFilters fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, FunctionMatchingFilters.class);
  }

  /**
   * Convert an instance of FunctionMatchingFilters to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

