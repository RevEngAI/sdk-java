/*
 * RevEng.AI API
 * RevEng.AI is an AI-powered binary analysis platform for reverse engineering and malware analysis. It provides similarity search across executable binaries and functions, AI-driven decompilation, dynamic execution analysis, firmware unpacking, and integration with external threat intelligence sources like VirusTotal.
 *
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package ai.reveng.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import org.openapitools.jackson.nullable.JsonNullable;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import ai.reveng.invoker.JSON;

/**
 * StartBatchMatchingInputBody
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen")
public class StartBatchMatchingInputBody {
  public static final String SERIALIZED_NAME_BINARY_IDS = "binary_ids";
  @SerializedName(SERIALIZED_NAME_BINARY_IDS)
  @javax.annotation.Nullable
  private List<Long> binaryIds;

  public static final String SERIALIZED_NAME_DEBUG_TYPES = "debug_types";
  @SerializedName(SERIALIZED_NAME_DEBUG_TYPES)
  @javax.annotation.Nullable
  private List<String> debugTypes;

  public static final String SERIALIZED_NAME_MIN_SIMILARITY = "min_similarity";
  @SerializedName(SERIALIZED_NAME_MIN_SIMILARITY)
  @javax.annotation.Nullable
  private Double minSimilarity;

  public static final String SERIALIZED_NAME_RESULTS_PER_FUNCTION = "results_per_function";
  @SerializedName(SERIALIZED_NAME_RESULTS_PER_FUNCTION)
  @javax.annotation.Nullable
  private Long resultsPerFunction;

  public StartBatchMatchingInputBody() {
  }

  public StartBatchMatchingInputBody binaryIds(@javax.annotation.Nullable List<Long> binaryIds) {
    this.binaryIds = binaryIds;
    return this;
  }

  public StartBatchMatchingInputBody addBinaryIdsItem(Long binaryIdsItem) {
    if (this.binaryIds == null) {
      this.binaryIds = new ArrayList<>();
    }
    this.binaryIds.add(binaryIdsItem);
    return this;
  }

  /**
   * Binary IDs to match the analysis against, one workflow per binary.
   * @return binaryIds
   */
  @javax.annotation.Nullable
  public List<Long> getBinaryIds() {
    return binaryIds;
  }

  public void setBinaryIds(@javax.annotation.Nullable List<Long> binaryIds) {
    this.binaryIds = binaryIds;
  }


  public StartBatchMatchingInputBody debugTypes(@javax.annotation.Nullable List<String> debugTypes) {
    this.debugTypes = debugTypes;
    return this;
  }

  public StartBatchMatchingInputBody addDebugTypesItem(String debugTypesItem) {
    if (this.debugTypes == null) {
      this.debugTypes = new ArrayList<>();
    }
    this.debugTypes.add(debugTypesItem);
    return this;
  }

  /**
   * Restrict matches to candidates with these debug source types. Defaults to [\&quot;SYSTEM\&quot;].
   * @return debugTypes
   */
  @javax.annotation.Nullable
  public List<String> getDebugTypes() {
    return debugTypes;
  }

  public void setDebugTypes(@javax.annotation.Nullable List<String> debugTypes) {
    this.debugTypes = debugTypes;
  }


  public StartBatchMatchingInputBody minSimilarity(@javax.annotation.Nullable Double minSimilarity) {
    this.minSimilarity = minSimilarity;
    return this;
  }

  /**
   * Similarity floor as a percentage. Defaults to 90.
   * minimum: 0
   * maximum: 100
   * @return minSimilarity
   */
  @javax.annotation.Nullable
  public Double getMinSimilarity() {
    return minSimilarity;
  }

  public void setMinSimilarity(@javax.annotation.Nullable Double minSimilarity) {
    this.minSimilarity = minSimilarity;
  }


  public StartBatchMatchingInputBody resultsPerFunction(@javax.annotation.Nullable Long resultsPerFunction) {
    this.resultsPerFunction = resultsPerFunction;
    return this;
  }

  /**
   * Max matches returned per source function. Defaults to 1.
   * minimum: 1
   * maximum: 30
   * @return resultsPerFunction
   */
  @javax.annotation.Nullable
  public Long getResultsPerFunction() {
    return resultsPerFunction;
  }

  public void setResultsPerFunction(@javax.annotation.Nullable Long resultsPerFunction) {
    this.resultsPerFunction = resultsPerFunction;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the StartBatchMatchingInputBody instance itself
   */
  public StartBatchMatchingInputBody putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }


  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    StartBatchMatchingInputBody startBatchMatchingInputBody = (StartBatchMatchingInputBody) o;
    return Objects.equals(this.binaryIds, startBatchMatchingInputBody.binaryIds) &&
        Objects.equals(this.debugTypes, startBatchMatchingInputBody.debugTypes) &&
        Objects.equals(this.minSimilarity, startBatchMatchingInputBody.minSimilarity) &&
        Objects.equals(this.resultsPerFunction, startBatchMatchingInputBody.resultsPerFunction)&&
        Objects.equals(this.additionalProperties, startBatchMatchingInputBody.additionalProperties);
  }

  private static <T> boolean equalsNullable(JsonNullable<T> a, JsonNullable<T> b) {
    return a == b || (a != null && b != null && a.isPresent() && b.isPresent() && Objects.deepEquals(a.get(), b.get()));
  }

  @Override
  public int hashCode() {
    return Objects.hash(binaryIds, debugTypes, minSimilarity, resultsPerFunction, additionalProperties);
  }

  private static <T> int hashCodeNullable(JsonNullable<T> a) {
    if (a == null) {
      return 1;
    }
    return a.isPresent() ? Arrays.deepHashCode(new Object[]{a.get()}) : 31;
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class StartBatchMatchingInputBody {\n");
    sb.append("    binaryIds: ").append(toIndentedString(binaryIds)).append("\n");
    sb.append("    debugTypes: ").append(toIndentedString(debugTypes)).append("\n");
    sb.append("    minSimilarity: ").append(toIndentedString(minSimilarity)).append("\n");
    sb.append("    resultsPerFunction: ").append(toIndentedString(resultsPerFunction)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    return o == null ? "null" : o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("binary_ids", "debug_types", "min_similarity", "results_per_function"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(Arrays.asList("binary_ids"));
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to StartBatchMatchingInputBody
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!StartBatchMatchingInputBody.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field(s) %s in StartBatchMatchingInputBody is not found in the empty JSON string", StartBatchMatchingInputBody.openapiRequiredFields.toString()));
        }
      }

      // check to make sure all required properties/fields are present in the JSON string
      for (String requiredField : StartBatchMatchingInputBody.openapiRequiredFields) {
        if (jsonElement.getAsJsonObject().get(requiredField) == null) {
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field `%s` is not found in the JSON string: %s", requiredField, jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // ensure the required json array is present
      if (jsonObj.get("binary_ids") == null) {
        throw new IllegalArgumentException("Expected the field `linkedContent` to be an array in the JSON string but got `null`");
      } else if (!jsonObj.get("binary_ids").isJsonArray() && !jsonObj.get("binary_ids").isJsonNull()) {
        throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `binary_ids` to be an array in the JSON string but got `%s`", jsonObj.get("binary_ids").toString()));
      }
      // ensure the optional json data is an array if present
      if (jsonObj.get("debug_types") != null && !jsonObj.get("debug_types").isJsonNull() && !jsonObj.get("debug_types").isJsonArray()) {
        throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `debug_types` to be an array in the JSON string but got `%s`", jsonObj.get("debug_types").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!StartBatchMatchingInputBody.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'StartBatchMatchingInputBody' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<StartBatchMatchingInputBody> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(StartBatchMatchingInputBody.class));

       return (TypeAdapter<T>) new TypeAdapter<StartBatchMatchingInputBody>() {
           @Override
           public void write(JsonWriter out, StartBatchMatchingInputBody value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additional properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else {
                   JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                   if (jsonElement.isJsonArray()) {
                     obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                   } else {
                     obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                   }
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public StartBatchMatchingInputBody read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             JsonObject jsonObj = jsonElement.getAsJsonObject();
             // store additional fields in the deserialized instance
             StartBatchMatchingInputBody instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of StartBatchMatchingInputBody given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of StartBatchMatchingInputBody
   * @throws IOException if the JSON string is invalid with respect to StartBatchMatchingInputBody
   */
  public static StartBatchMatchingInputBody fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, StartBatchMatchingInputBody.class);
  }

  /**
   * Convert an instance of StartBatchMatchingInputBody to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

