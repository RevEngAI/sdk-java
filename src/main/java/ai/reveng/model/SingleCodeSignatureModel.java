/*
 * RevEng.AI API
 * RevEng.AI is Similarity Search Engine for executable binaries
 *
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package ai.reveng.model;

import java.util.Objects;
import ai.reveng.model.SingleCodeCertificateModel;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import ai.reveng.invoker.JSON;

/**
 * SingleCodeSignatureModel
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen"", comments = "Generator version: 7.15.0")
public class SingleCodeSignatureModel {
  public static final String SERIALIZED_NAME_CERTIFICATES = "certificates";
  @SerializedName(SERIALIZED_NAME_CERTIFICATES)
  @javax.annotation.Nonnull
  private List<SingleCodeCertificateModel> certificates = new ArrayList<>();

  public static final String SERIALIZED_NAME_AUTHENTICODE_DIGEST = "authenticode_digest";
  @SerializedName(SERIALIZED_NAME_AUTHENTICODE_DIGEST)
  @javax.annotation.Nonnull
  private String authenticodeDigest;

  public SingleCodeSignatureModel() {
  }

  public SingleCodeSignatureModel certificates(@javax.annotation.Nonnull List<SingleCodeCertificateModel> certificates) {
    this.certificates = certificates;
    return this;
  }

  public SingleCodeSignatureModel addCertificatesItem(SingleCodeCertificateModel certificatesItem) {
    if (this.certificates == null) {
      this.certificates = new ArrayList<>();
    }
    this.certificates.add(certificatesItem);
    return this;
  }

  /**
   * Get certificates
   * @return certificates
   */
  @javax.annotation.Nonnull
  public List<SingleCodeCertificateModel> getCertificates() {
    return certificates;
  }

  public void setCertificates(@javax.annotation.Nonnull List<SingleCodeCertificateModel> certificates) {
    this.certificates = certificates;
  }


  public SingleCodeSignatureModel authenticodeDigest(@javax.annotation.Nonnull String authenticodeDigest) {
    this.authenticodeDigest = authenticodeDigest;
    return this;
  }

  /**
   * Get authenticodeDigest
   * @return authenticodeDigest
   */
  @javax.annotation.Nonnull
  public String getAuthenticodeDigest() {
    return authenticodeDigest;
  }

  public void setAuthenticodeDigest(@javax.annotation.Nonnull String authenticodeDigest) {
    this.authenticodeDigest = authenticodeDigest;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    SingleCodeSignatureModel singleCodeSignatureModel = (SingleCodeSignatureModel) o;
    return Objects.equals(this.certificates, singleCodeSignatureModel.certificates) &&
        Objects.equals(this.authenticodeDigest, singleCodeSignatureModel.authenticodeDigest);
  }

  @Override
  public int hashCode() {
    return Objects.hash(certificates, authenticodeDigest);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class SingleCodeSignatureModel {\n");
    sb.append("    certificates: ").append(toIndentedString(certificates)).append("\n");
    sb.append("    authenticodeDigest: ").append(toIndentedString(authenticodeDigest)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("certificates", "authenticode_digest"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(Arrays.asList("certificates", "authenticode_digest"));
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to SingleCodeSignatureModel
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!SingleCodeSignatureModel.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in SingleCodeSignatureModel is not found in the empty JSON string", SingleCodeSignatureModel.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!SingleCodeSignatureModel.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `SingleCodeSignatureModel` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }

      // check to make sure all required properties/fields are present in the JSON string
      for (String requiredField : SingleCodeSignatureModel.openapiRequiredFields) {
        if (jsonElement.getAsJsonObject().get(requiredField) == null) {
          throw new IllegalArgumentException(String.format("The required field `%s` is not found in the JSON string: %s", requiredField, jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // ensure the json data is an array
      if (!jsonObj.get("certificates").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `certificates` to be an array in the JSON string but got `%s`", jsonObj.get("certificates").toString()));
      }

      JsonArray jsonArraycertificates = jsonObj.getAsJsonArray("certificates");
      // validate the required field `certificates` (array)
      for (int i = 0; i < jsonArraycertificates.size(); i++) {
        SingleCodeCertificateModel.validateJsonElement(jsonArraycertificates.get(i));
      };
      if (!jsonObj.get("authenticode_digest").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `authenticode_digest` to be a primitive type in the JSON string but got `%s`", jsonObj.get("authenticode_digest").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!SingleCodeSignatureModel.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'SingleCodeSignatureModel' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<SingleCodeSignatureModel> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(SingleCodeSignatureModel.class));

       return (TypeAdapter<T>) new TypeAdapter<SingleCodeSignatureModel>() {
           @Override
           public void write(JsonWriter out, SingleCodeSignatureModel value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public SingleCodeSignatureModel read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of SingleCodeSignatureModel given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of SingleCodeSignatureModel
   * @throws IOException if the JSON string is invalid with respect to SingleCodeSignatureModel
   */
  public static SingleCodeSignatureModel fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, SingleCodeSignatureModel.class);
  }

  /**
   * Convert an instance of SingleCodeSignatureModel to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

