/*
 * RevEng.AI API
 * RevEng.AI is an AI-powered binary analysis platform for reverse engineering and malware analysis. It provides similarity search across executable binaries and functions, AI-driven decompilation, dynamic execution analysis, firmware unpacking, and integration with external threat intelligence sources like VirusTotal.
 *
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package ai.reveng.model;

import java.util.Objects;
import ai.reveng.model.XrefFromBody;
import ai.reveng.model.XrefIntoBody;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import ai.reveng.invoker.JSON;

/**
 * AnalysisXrefOutputBody
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen")
public class AnalysisXrefOutputBody {
  public static final String SERIALIZED_NAME_XREF_FROM_LIST = "xref_from_list";
  @SerializedName(SERIALIZED_NAME_XREF_FROM_LIST)
  @javax.annotation.Nullable
  private List<XrefFromBody> xrefFromList;

  public static final String SERIALIZED_NAME_XREF_TO_LIST = "xref_to_list";
  @SerializedName(SERIALIZED_NAME_XREF_TO_LIST)
  @javax.annotation.Nullable
  private List<XrefIntoBody> xrefToList;

  public AnalysisXrefOutputBody() {
  }

  public AnalysisXrefOutputBody xrefFromList(@javax.annotation.Nullable List<XrefFromBody> xrefFromList) {
    this.xrefFromList = xrefFromList;
    return this;
  }

  public AnalysisXrefOutputBody addXrefFromListItem(XrefFromBody xrefFromListItem) {
    if (this.xrefFromList == null) {
      this.xrefFromList = new ArrayList<>();
    }
    this.xrefFromList.add(xrefFromListItem);
    return this;
  }

  /**
   * Xrefs that originate at the queried vaddr, one per target address
   * @return xrefFromList
   */
  @javax.annotation.Nullable
  public List<XrefFromBody> getXrefFromList() {
    return xrefFromList;
  }

  public void setXrefFromList(@javax.annotation.Nullable List<XrefFromBody> xrefFromList) {
    this.xrefFromList = xrefFromList;
  }


  public AnalysisXrefOutputBody xrefToList(@javax.annotation.Nullable List<XrefIntoBody> xrefToList) {
    this.xrefToList = xrefToList;
    return this;
  }

  public AnalysisXrefOutputBody addXrefToListItem(XrefIntoBody xrefToListItem) {
    if (this.xrefToList == null) {
      this.xrefToList = new ArrayList<>();
    }
    this.xrefToList.add(xrefToListItem);
    return this;
  }

  /**
   * Xrefs that target the queried vaddr, one per referencing address
   * @return xrefToList
   */
  @javax.annotation.Nullable
  public List<XrefIntoBody> getXrefToList() {
    return xrefToList;
  }

  public void setXrefToList(@javax.annotation.Nullable List<XrefIntoBody> xrefToList) {
    this.xrefToList = xrefToList;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the AnalysisXrefOutputBody instance itself
   */
  public AnalysisXrefOutputBody putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }


  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    AnalysisXrefOutputBody analysisXrefOutputBody = (AnalysisXrefOutputBody) o;
    return Objects.equals(this.xrefFromList, analysisXrefOutputBody.xrefFromList) &&
        Objects.equals(this.xrefToList, analysisXrefOutputBody.xrefToList)&&
        Objects.equals(this.additionalProperties, analysisXrefOutputBody.additionalProperties);
  }

  @Override
  public int hashCode() {
    return Objects.hash(xrefFromList, xrefToList, additionalProperties);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class AnalysisXrefOutputBody {\n");
    sb.append("    xrefFromList: ").append(toIndentedString(xrefFromList)).append("\n");
    sb.append("    xrefToList: ").append(toIndentedString(xrefToList)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    return o == null ? "null" : o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("xref_from_list", "xref_to_list"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(Arrays.asList("xref_from_list", "xref_to_list"));
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to AnalysisXrefOutputBody
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!AnalysisXrefOutputBody.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field(s) %s in AnalysisXrefOutputBody is not found in the empty JSON string", AnalysisXrefOutputBody.openapiRequiredFields.toString()));
        }
      }

      // check to make sure all required properties/fields are present in the JSON string
      for (String requiredField : AnalysisXrefOutputBody.openapiRequiredFields) {
        if (jsonElement.getAsJsonObject().get(requiredField) == null) {
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field `%s` is not found in the JSON string: %s", requiredField, jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // ensure the required json array is present
      if (jsonObj.get("xref_from_list") == null) {
        throw new IllegalArgumentException("Expected the field `linkedContent` to be an array in the JSON string but got `null`");
      } else if (!jsonObj.get("xref_from_list").isJsonArray() && !jsonObj.get("xref_from_list").isJsonNull()) {
        throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `xref_from_list` to be an array in the JSON string but got `%s`", jsonObj.get("xref_from_list").toString()));
      }
      // ensure the required json array is present
      if (jsonObj.get("xref_to_list") == null) {
        throw new IllegalArgumentException("Expected the field `linkedContent` to be an array in the JSON string but got `null`");
      } else if (!jsonObj.get("xref_to_list").isJsonArray() && !jsonObj.get("xref_to_list").isJsonNull()) {
        throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `xref_to_list` to be an array in the JSON string but got `%s`", jsonObj.get("xref_to_list").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!AnalysisXrefOutputBody.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'AnalysisXrefOutputBody' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<AnalysisXrefOutputBody> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(AnalysisXrefOutputBody.class));

       return (TypeAdapter<T>) new TypeAdapter<AnalysisXrefOutputBody>() {
           @Override
           public void write(JsonWriter out, AnalysisXrefOutputBody value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additional properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else {
                   JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                   if (jsonElement.isJsonArray()) {
                     obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                   } else {
                     obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                   }
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public AnalysisXrefOutputBody read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             JsonObject jsonObj = jsonElement.getAsJsonObject();
             // store additional fields in the deserialized instance
             AnalysisXrefOutputBody instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of AnalysisXrefOutputBody given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of AnalysisXrefOutputBody
   * @throws IOException if the JSON string is invalid with respect to AnalysisXrefOutputBody
   */
  public static AnalysisXrefOutputBody fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, AnalysisXrefOutputBody.class);
  }

  /**
   * Convert an instance of AnalysisXrefOutputBody to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

