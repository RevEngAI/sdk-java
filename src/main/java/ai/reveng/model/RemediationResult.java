/*
 * RevEng.AI API
 * RevEng.AI is an AI-powered binary analysis platform for reverse engineering and malware analysis. It provides similarity search across executable binaries and functions, AI-driven decompilation, dynamic execution analysis, firmware unpacking, and integration with external threat intelligence sources like VirusTotal.
 *
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package ai.reveng.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import ai.reveng.invoker.JSON;

/**
 * RemediationResult
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen")
public class RemediationResult {
  public static final String SERIALIZED_NAME_SNORT_RULES = "snort_rules";
  @SerializedName(SERIALIZED_NAME_SNORT_RULES)
  @javax.annotation.Nullable
  private List<String> snortRules;

  public static final String SERIALIZED_NAME_STIX_RULES = "stix_rules";
  @SerializedName(SERIALIZED_NAME_STIX_RULES)
  @javax.annotation.Nullable
  private List<String> stixRules;

  public static final String SERIALIZED_NAME_YARA_RULES = "yara_rules";
  @SerializedName(SERIALIZED_NAME_YARA_RULES)
  @javax.annotation.Nullable
  private List<String> yaraRules;

  public RemediationResult() {
  }

  public RemediationResult snortRules(@javax.annotation.Nullable List<String> snortRules) {
    this.snortRules = snortRules;
    return this;
  }

  public RemediationResult addSnortRulesItem(String snortRulesItem) {
    if (this.snortRules == null) {
      this.snortRules = new ArrayList<>();
    }
    this.snortRules.add(snortRulesItem);
    return this;
  }

  /**
   * Generated Snort rules
   * @return snortRules
   */
  @javax.annotation.Nullable
  public List<String> getSnortRules() {
    return snortRules;
  }

  public void setSnortRules(@javax.annotation.Nullable List<String> snortRules) {
    this.snortRules = snortRules;
  }


  public RemediationResult stixRules(@javax.annotation.Nullable List<String> stixRules) {
    this.stixRules = stixRules;
    return this;
  }

  public RemediationResult addStixRulesItem(String stixRulesItem) {
    if (this.stixRules == null) {
      this.stixRules = new ArrayList<>();
    }
    this.stixRules.add(stixRulesItem);
    return this;
  }

  /**
   * Generated STIX rules
   * @return stixRules
   */
  @javax.annotation.Nullable
  public List<String> getStixRules() {
    return stixRules;
  }

  public void setStixRules(@javax.annotation.Nullable List<String> stixRules) {
    this.stixRules = stixRules;
  }


  public RemediationResult yaraRules(@javax.annotation.Nullable List<String> yaraRules) {
    this.yaraRules = yaraRules;
    return this;
  }

  public RemediationResult addYaraRulesItem(String yaraRulesItem) {
    if (this.yaraRules == null) {
      this.yaraRules = new ArrayList<>();
    }
    this.yaraRules.add(yaraRulesItem);
    return this;
  }

  /**
   * Generated YARA rules
   * @return yaraRules
   */
  @javax.annotation.Nullable
  public List<String> getYaraRules() {
    return yaraRules;
  }

  public void setYaraRules(@javax.annotation.Nullable List<String> yaraRules) {
    this.yaraRules = yaraRules;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the RemediationResult instance itself
   */
  public RemediationResult putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }


  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    RemediationResult remediationResult = (RemediationResult) o;
    return Objects.equals(this.snortRules, remediationResult.snortRules) &&
        Objects.equals(this.stixRules, remediationResult.stixRules) &&
        Objects.equals(this.yaraRules, remediationResult.yaraRules)&&
        Objects.equals(this.additionalProperties, remediationResult.additionalProperties);
  }

  @Override
  public int hashCode() {
    return Objects.hash(snortRules, stixRules, yaraRules, additionalProperties);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class RemediationResult {\n");
    sb.append("    snortRules: ").append(toIndentedString(snortRules)).append("\n");
    sb.append("    stixRules: ").append(toIndentedString(stixRules)).append("\n");
    sb.append("    yaraRules: ").append(toIndentedString(yaraRules)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    return o == null ? "null" : o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("snort_rules", "stix_rules", "yara_rules"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(Arrays.asList("snort_rules", "stix_rules", "yara_rules"));
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to RemediationResult
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!RemediationResult.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field(s) %s in RemediationResult is not found in the empty JSON string", RemediationResult.openapiRequiredFields.toString()));
        }
      }

      // check to make sure all required properties/fields are present in the JSON string
      for (String requiredField : RemediationResult.openapiRequiredFields) {
        if (jsonElement.getAsJsonObject().get(requiredField) == null) {
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field `%s` is not found in the JSON string: %s", requiredField, jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // ensure the required json array is present
      if (jsonObj.get("snort_rules") == null) {
        throw new IllegalArgumentException("Expected the field `linkedContent` to be an array in the JSON string but got `null`");
      } else if (!jsonObj.get("snort_rules").isJsonArray() && !jsonObj.get("snort_rules").isJsonNull()) {
        throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `snort_rules` to be an array in the JSON string but got `%s`", jsonObj.get("snort_rules").toString()));
      }
      // ensure the required json array is present
      if (jsonObj.get("stix_rules") == null) {
        throw new IllegalArgumentException("Expected the field `linkedContent` to be an array in the JSON string but got `null`");
      } else if (!jsonObj.get("stix_rules").isJsonArray() && !jsonObj.get("stix_rules").isJsonNull()) {
        throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `stix_rules` to be an array in the JSON string but got `%s`", jsonObj.get("stix_rules").toString()));
      }
      // ensure the required json array is present
      if (jsonObj.get("yara_rules") == null) {
        throw new IllegalArgumentException("Expected the field `linkedContent` to be an array in the JSON string but got `null`");
      } else if (!jsonObj.get("yara_rules").isJsonArray() && !jsonObj.get("yara_rules").isJsonNull()) {
        throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `yara_rules` to be an array in the JSON string but got `%s`", jsonObj.get("yara_rules").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!RemediationResult.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'RemediationResult' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<RemediationResult> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(RemediationResult.class));

       return (TypeAdapter<T>) new TypeAdapter<RemediationResult>() {
           @Override
           public void write(JsonWriter out, RemediationResult value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additional properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else {
                   JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                   if (jsonElement.isJsonArray()) {
                     obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                   } else {
                     obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                   }
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public RemediationResult read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             JsonObject jsonObj = jsonElement.getAsJsonObject();
             // store additional fields in the deserialized instance
             RemediationResult instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of RemediationResult given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of RemediationResult
   * @throws IOException if the JSON string is invalid with respect to RemediationResult
   */
  public static RemediationResult fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, RemediationResult.class);
  }

  /**
   * Convert an instance of RemediationResult to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

