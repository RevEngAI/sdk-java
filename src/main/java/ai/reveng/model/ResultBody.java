/*
 * RevEng.AI API
 * RevEng.AI is an AI-powered binary analysis platform for reverse engineering and malware analysis. It provides similarity search across executable binaries and functions, AI-driven decompilation, dynamic execution analysis, firmware unpacking, and integration with external threat intelligence sources like VirusTotal.
 *
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package ai.reveng.model;

import java.util.Objects;
import ai.reveng.model.ExtractedBinary;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import ai.reveng.invoker.JSON;

/**
 * ResultBody
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen")
public class ResultBody {
  public static final String SERIALIZED_NAME_BINARIES = "binaries";
  @SerializedName(SERIALIZED_NAME_BINARIES)
  @javax.annotation.Nullable
  private List<ExtractedBinary> binaries;

  public static final String SERIALIZED_NAME_EXTRACTION_DEPTH = "extraction_depth";
  @SerializedName(SERIALIZED_NAME_EXTRACTION_DEPTH)
  @javax.annotation.Nonnull
  private Long extractionDepth;

  public static final String SERIALIZED_NAME_FILENAME_TO_EXTRACTION_FAILURE = "filename_to_extraction_failure";
  @SerializedName(SERIALIZED_NAME_FILENAME_TO_EXTRACTION_FAILURE)
  @javax.annotation.Nonnull
  private Map<String, String> filenameToExtractionFailure = new HashMap<>();

  public static final String SERIALIZED_NAME_SKIPPED_FILES = "skipped_files";
  @SerializedName(SERIALIZED_NAME_SKIPPED_FILES)
  @javax.annotation.Nonnull
  private Long skippedFiles;

  public ResultBody() {
  }

  public ResultBody binaries(@javax.annotation.Nullable List<ExtractedBinary> binaries) {
    this.binaries = binaries;
    return this;
  }

  public ResultBody addBinariesItem(ExtractedBinary binariesItem) {
    if (this.binaries == null) {
      this.binaries = new ArrayList<>();
    }
    this.binaries.add(binariesItem);
    return this;
  }

  /**
   * Child binaries recovered from the extraction.
   * @return binaries
   */
  @javax.annotation.Nullable
  public List<ExtractedBinary> getBinaries() {
    return binaries;
  }

  public void setBinaries(@javax.annotation.Nullable List<ExtractedBinary> binaries) {
    this.binaries = binaries;
  }


  public ResultBody extractionDepth(@javax.annotation.Nonnull Long extractionDepth) {
    this.extractionDepth = extractionDepth;
    return this;
  }

  /**
   * Number of nested-archive extraction passes taken.
   * @return extractionDepth
   */
  @javax.annotation.Nonnull
  public Long getExtractionDepth() {
    return extractionDepth;
  }

  public void setExtractionDepth(@javax.annotation.Nonnull Long extractionDepth) {
    this.extractionDepth = extractionDepth;
  }


  public ResultBody filenameToExtractionFailure(@javax.annotation.Nonnull Map<String, String> filenameToExtractionFailure) {
    this.filenameToExtractionFailure = filenameToExtractionFailure;
    return this;
  }

  public ResultBody putFilenameToExtractionFailureItem(String key, String filenameToExtractionFailureItem) {
    if (this.filenameToExtractionFailure == null) {
      this.filenameToExtractionFailure = new HashMap<>();
    }
    this.filenameToExtractionFailure.put(key, filenameToExtractionFailureItem);
    return this;
  }

  /**
   * Per-file extraction failures, keyed by filename.
   * @return filenameToExtractionFailure
   */
  @javax.annotation.Nonnull
  public Map<String, String> getFilenameToExtractionFailure() {
    return filenameToExtractionFailure;
  }

  public void setFilenameToExtractionFailure(@javax.annotation.Nonnull Map<String, String> filenameToExtractionFailure) {
    this.filenameToExtractionFailure = filenameToExtractionFailure;
  }


  public ResultBody skippedFiles(@javax.annotation.Nonnull Long skippedFiles) {
    this.skippedFiles = skippedFiles;
    return this;
  }

  /**
   * Files skipped because they were not recognised as binaries.
   * @return skippedFiles
   */
  @javax.annotation.Nonnull
  public Long getSkippedFiles() {
    return skippedFiles;
  }

  public void setSkippedFiles(@javax.annotation.Nonnull Long skippedFiles) {
    this.skippedFiles = skippedFiles;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the ResultBody instance itself
   */
  public ResultBody putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }


  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    ResultBody resultBody = (ResultBody) o;
    return Objects.equals(this.binaries, resultBody.binaries) &&
        Objects.equals(this.extractionDepth, resultBody.extractionDepth) &&
        Objects.equals(this.filenameToExtractionFailure, resultBody.filenameToExtractionFailure) &&
        Objects.equals(this.skippedFiles, resultBody.skippedFiles)&&
        Objects.equals(this.additionalProperties, resultBody.additionalProperties);
  }

  @Override
  public int hashCode() {
    return Objects.hash(binaries, extractionDepth, filenameToExtractionFailure, skippedFiles, additionalProperties);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class ResultBody {\n");
    sb.append("    binaries: ").append(toIndentedString(binaries)).append("\n");
    sb.append("    extractionDepth: ").append(toIndentedString(extractionDepth)).append("\n");
    sb.append("    filenameToExtractionFailure: ").append(toIndentedString(filenameToExtractionFailure)).append("\n");
    sb.append("    skippedFiles: ").append(toIndentedString(skippedFiles)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    return o == null ? "null" : o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("binaries", "extraction_depth", "filename_to_extraction_failure", "skipped_files"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(Arrays.asList("binaries", "extraction_depth", "filename_to_extraction_failure", "skipped_files"));
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to ResultBody
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!ResultBody.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field(s) %s in ResultBody is not found in the empty JSON string", ResultBody.openapiRequiredFields.toString()));
        }
      }

      // check to make sure all required properties/fields are present in the JSON string
      for (String requiredField : ResultBody.openapiRequiredFields) {
        if (jsonElement.getAsJsonObject().get(requiredField) == null) {
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field `%s` is not found in the JSON string: %s", requiredField, jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (jsonObj.get("binaries") != null && !jsonObj.get("binaries").isJsonNull()) {
        if (!jsonObj.get("binaries").isJsonArray()) {
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `binaries` to be an array in the JSON string but got `%s`", jsonObj.get("binaries").toString()));
        }
        JsonArray jsonArraybinaries = jsonObj.getAsJsonArray("binaries");
        // validate the required field `binaries` (array)
        for (int i = 0; i < jsonArraybinaries.size(); i++) {
          ExtractedBinary.validateJsonElement(jsonArraybinaries.get(i));
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!ResultBody.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'ResultBody' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<ResultBody> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(ResultBody.class));

       return (TypeAdapter<T>) new TypeAdapter<ResultBody>() {
           @Override
           public void write(JsonWriter out, ResultBody value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additional properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else {
                   JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                   if (jsonElement.isJsonArray()) {
                     obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                   } else {
                     obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                   }
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public ResultBody read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             JsonObject jsonObj = jsonElement.getAsJsonObject();
             // store additional fields in the deserialized instance
             ResultBody instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of ResultBody given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of ResultBody
   * @throws IOException if the JSON string is invalid with respect to ResultBody
   */
  public static ResultBody fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, ResultBody.class);
  }

  /**
   * Convert an instance of ResultBody to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

